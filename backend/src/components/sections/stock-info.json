{
  "collectionName": "components_sections_stock_infos",
  "info": {
    "displayName": "Stock Info",
    "description": "Stock Info: static labelled rows + notes (widget optional)"
  },
  "options": {},
  "attributes": {
    "heading": {
      "type": "string",
      "description": "Section heading."
    },
    "rows": {
      "type": "component",
      "repeatable": true,
      "component": "shared.key-value",
      "description": "Rows of stock info, e.g. Ticker → PWRL, NAV → $700.9M."
    },
    "notes": {
      "type": "richtext",
      "description": "Small print beneath the rows (optional)."
    },
    "widgetId": {
      "type": "string",
      "required": false,
      "description": "Optional third-party stock-widget id (leave empty unless a developer provides one)."
    },
    "theme": {
      "type": "enumeration",
      "enum": [
        "light",
        "dark",
        "deep"
      ],
      "default": "light",
      "description": "Colour style: light, dark, or deep."
    }
  }
}