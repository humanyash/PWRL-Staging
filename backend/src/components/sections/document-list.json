{
  "collectionName": "components_sections_document_lists",
  "info": {
    "displayName": "Document List",
    "description": "Heading + kind; auto-pulls SEC filings or fund documents"
  },
  "options": {},
  "attributes": {
    "heading": {
      "type": "string",
      "description": "Section heading."
    },
    "kind": {
      "type": "enumeration",
      "enum": [
        "filings",
        "fund-docs"
      ],
      "required": true,
      "description": "Which list to show: 'filings' = SEC filings (pulled automatically from EDGAR) or 'fund-docs' = the Fund Documents (PDFs) you upload."
    },
    "documents": {
      "type": "component",
      "repeatable": true,
      "component": "shared.footer-link",
      "description": "Manual document links (only used if not pulling from Fund Documents)."
    },
    "note": {
      "type": "text",
      "description": "Small note under the list (optional)."
    },
    "emptyText": {
      "type": "string",
      "description": "Message shown if the list is empty (optional)."
    }
  }
}