{
  "collectionName": "components_shared_footer_links",
  "info": {
    "displayName": "Footer Link",
    "description": "A labelled footer/social link"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true,
      "description": "Text shown for the link (for social links, use the platform name so the right icon appears)."
    },
    "href": {
      "type": "string",
      "required": true,
      "description": "Where the link goes. Use /page for internal pages or a full https:// address for external."
    },
    "group": {
      "type": "string",
      "description": "Optional grouping label (leave empty unless a developer asks)."
    }
  }
}
