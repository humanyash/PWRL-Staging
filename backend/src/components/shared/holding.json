{
  "collectionName": "components_shared_holdings",
  "info": {
    "displayName": "Holding",
    "description": "A single portfolio holding row"
  },
  "options": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "description": "Company or sector name, e.g. SpaceX or Aerospace & Defense."
    },
    "allocation": {
      "type": "string",
      "required": true,
      "description": "Percentage of the fund, written with the % sign, e.g. 19.37%."
    },
    "sector": {
      "type": "string",
      "description": "Optional sector label (leave empty if not needed)."
    }
  }
}
