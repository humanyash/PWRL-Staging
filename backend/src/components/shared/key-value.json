{
  "collectionName": "components_shared_key_values",
  "info": {
    "displayName": "Key Value",
    "description": "A labelled value row (e.g. Stock Info: Ticker → PWRL)"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true,
      "description": "Row label, e.g. Ticker or Net Asset Value."
    },
    "value": {
      "type": "string",
      "required": true,
      "description": "Row value, e.g. PWRL or $700.9M."
    }
  }
}
