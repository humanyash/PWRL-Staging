{
  "kind": "singleType",
  "collectionName": "portfolio_snapshots",
  "info": {
    "singularName": "portfolio-snapshot",
    "pluralName": "portfolio-snapshots",
    "displayName": "Fund Portfolio",
    "description": "Holdings table on /fund. Update the 'As of date' and holdings each month."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "asOfDate": {
      "type": "date",
      "required": true,
      "description": "The date these holdings are accurate as of (shown as 'as of ...')."
    },
    "intro": {
      "type": "text",
      "description": "Short paragraph shown above the holdings table (optional)."
    },
    "holdings": {
      "type": "component",
      "repeatable": true,
      "component": "shared.holding",
      "required": true,
      "description": "One row per company: name and allocation percentage."
    },
    "footnotes": {
      "type": "richtext",
      "description": "Small print shown beneath the table (optional)."
    }
  }
}
